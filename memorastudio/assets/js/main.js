const translations = {
  "pt-BR": {
    "meta.description": "Memora Studio — estúdio de ensaio, gravação, audiovisual e produção para músicos.",
    "meta.title": "Memora Studio | Seu tempo. Seu som. Seu espaço.",
    "accessibility.skipContent": "Pular para o conteúdo",
    "brand.homeLabel": "Memora Studio — início",
    "nav.mainLabel": "Navegação principal",
    "nav.mobileLabel": "Navegação móvel",
    "nav.studio": "Studio",
    "nav.rooms": "Salas",
    "nav.services": "Serviços",
    "nav.sessions": "Sessions",
    "nav.plans": "Planos",
    "nav.connect": "Connect",
    "language.label": "Idioma",
    "language.selectorLabel": "Selecionar idioma",
    "language.ptLabel": "Usar português",
    "language.enLabel": "Usar inglês",
    "actions.book": "Agendar",
    "actions.bookTime": "Agendar horário",
    "actions.bookNow": "Agendar agora",
    "actions.discoverStudio": "Conhecer o Studio",
    "actions.learnMore": "Saiba mais",
    "actions.continue": "Continuar",
    "actions.gotIt": "Entendi",
    "actions.clientAreaLabel": "Acessar área do cliente",
    "menu.open": "Abrir menu",
    "menu.close": "Fechar menu",
    "toast.clientArea": "Área do cliente em breve.",
    "toast.sessionsSoon": "Novas sessions serão publicadas em breve.",
    "toast.faqSoon": "Perguntas frequentes em breve.",
    "toast.policiesSoon": "Políticas em breve.",
    "toast.socialUnavailable": "Link oficial ainda não configurado.",
    "hero.imageAlt": "Sala do Memora Studio com bateria, amplificadores e equipamentos de áudio",
    "hero.title": "<span class=\"hero-line hero-line-light\">Seu tempo.</span><span class=\"hero-line hero-line-light\">Seu som.</span><span class=\"hero-line\">Seu espaço.</span>",
    "hero.text": "Um estúdio completo para ensaiar,<br>gravar, criar e se conectar.<br>Tecnologia, conforto e organização<br>para você focar no que importa:<br>a música.",
    "hero.scroll": "Role para explorar",
    "services.heading": "Serviços do Memora Studio",
    "services.rehearsal.title": "Ensaio",
    "services.rehearsal.text": "Salas equipadas e acusticamente tratadas para você e sua banda.",
    "services.recording.title": "Gravação",
    "services.recording.text": "Grave seu ensaio ou produção com qualidade profissional.",
    "services.audiovisual.title": "Audiovisual",
    "services.audiovisual.text": "Registre seus ensaios e produções com vídeo em alta qualidade.",
    "services.production.title": "Produção",
    "services.production.text": "Mixagem, edição e suporte técnico para levar sua música mais longe.",
    "experience.eyebrow": "A experiência Memora",
    "experience.title": "Mais que um estúdio.<br><span>Uma experiência.</span>",
    "experience.text": "Eliminamos a bagunça para você focar na música. Agendamento fácil, divisão de pagamentos, créditos, organização de arquivos e conexão com músicos e profissionais.",
    "experience.quote": "Você cuida da música.<br><strong>O Studio cuida do resto.</strong>",
    "values.label": "Valores do Memora Studio",
    "values.connection.title": "Conexão",
    "values.connection.text": "Conectamos músicos, serviços e oportunidades.",
    "values.punctuality.title": "Pontualidade",
    "values.punctuality.text": "Respeitamos seu tempo e transformamos isso em benefícios.",
    "values.innovation.title": "Inovação",
    "values.innovation.text": "Tecnologia para tornar tudo mais simples e eficiente.",
    "values.accessibility.title": "Acessibilidade",
    "values.accessibility.text": "Música para todos. Espaço aberto à diversidade.",
    "values.comfort.title": "Conforto",
    "values.comfort.text": "Ambiente preparado para você se sentir bem e criar livremente.",
    "booking.eyebrow": "Agenda online",
    "booking.title": "Reserve em<br><span>poucos cliques</span>",
    "booking.text": "Veja horários disponíveis, escolha sua sala, adicione serviços, divida com a banda e pronto. Tudo em um só lugar.",
    "booking.legendLabel": "Legenda da agenda",
    "booking.available": "Disponível",
    "booking.occupied": "Ocupado",
    "booking.selected": "Selecionado",
    "booking.chooseDate": "Escolha uma data",
    "booking.previousDay": "Dia anterior",
    "booking.nextDay": "Próximo dia",
    "booking.selectDate": "Selecionar data",
    "booking.scheduleLabel": "Tabela de horários; deslize horizontalmente se necessário",
    "booking.caption": "Horários disponíveis por sala",
    "booking.yourBooking": "Sua reserva",
    "booking.total": "Total",
    "booking.timeColumn": "Horário",
    "booking.hours": "{count} horas",
    "booking.hour": "{count} hora",
    "booking.timeRange": "{date} — {start} às {end}",
    "booking.confirmationCopy": "{room}, {date}, das {start} às {end} — {total}.",
    "booking.slotLabel": "{room}, {time}, {status}",
    "benefits.heading": "Diferenciais do Memora Studio",
    "benefits.split.title": "Divida com a banda",
    "benefits.split.text": "Convide os integrantes e cada um paga sua parte.",
    "benefits.credits.text": "Ganhe créditos com pontualidade, pacotes e indicações.",
    "benefits.files.title": "Arquivos organizados",
    "benefits.files.text": "Todas as gravações e mídias da sua banda em um só lugar.",
    "benefits.musicians.title": "Encontre músicos",
    "benefits.musicians.text": "Precisa de alguém? Conecte-se com músicos e profissionais.",
    "sessions.recordedAt": "Gravado no",
    "sessions.viewAll": "Ver todas as sessões",
    "sessions.playLabel": "Reproduzir {name}",
    "sessions.imageAlt": "{name} em sessão no Memora Studio",
    "cta.imageAlt": "Guitarra e pedais no palco do Memora Studio",
    "cta.eyebrow": "Sua próxima sessão começa aqui",
    "cta.title": "Pronto para levar<br>sua música <span>mais longe?</span>",
    "cta.text": "Agende agora e viva a experiência Memora Studio.",
    "footer.description": "Estúdio de ensaio, gravação e produção pensado para músicos que levam a música a sério.",
    "footer.socialLabel": "Redes sociais",
    "footer.navigation": "Navegação",
    "footer.theStudio": "O Studio",
    "footer.mixing": "Mixagem",
    "footer.help": "Ajuda",
    "footer.faq": "Perguntas Frequentes",
    "footer.howItWorks": "Como Funciona",
    "footer.policies": "Políticas",
    "footer.contact": "Contato",
    "footer.directions": "Como chegar",
    "footer.copyright": "© 2026 Memora Studio. Todos os direitos reservados.",
    "footer.tagline": "Feito para quem leva a música a sério.",
    "rooms.eyebrow": "Estrutura profissional",
    "rooms.title": "Conheça nossas salas",
    "rooms.name": "Sala {number}",
    "rooms.imageAlt": "{room} do Memora Studio",
    "rooms.feature.drums": "Bateria",
    "rooms.feature.amplifiers": "Amplificadores",
    "rooms.feature.pa": "PA",
    "rooms.feature.microphones": "Microfones",
    "rooms.feature.airConditioning": "Ar-condicionado",
    "rooms.feature.acousticTreatment": "Tratamento acústico",
    "schedule.service.multitrack": "Gravação multipista",
    "schedule.service.cameras": "2 câmeras",
    "schedule.service.technician": "Técnico de áudio",
    "session.type.live": "Sessão ao vivo",
    "session.type.rehearsalRecording": "Ensaio + Gravação",
    "session.type.rehearsalVideo": "Ensaio + Vídeo",
    "modal.close": "Fechar modal",
    "video.title": "Vídeo em breve.",
    "video.text": "Estamos preparando este registro para você.",
    "confirmation.eyebrow": "Reserva selecionada",
    "confirmation.title": "Tudo certo para continuar.",
    "confirmation.note": "Esta é uma demonstração. A integração de pagamento e confirmação será conectada ao backend futuramente."
  },
  en: {
    "meta.description": "Memora Studio — a rehearsal, recording, audiovisual, and production studio for musicians.",
    "meta.title": "Memora Studio | Your time. Your sound. Your space.",
    "accessibility.skipContent": "Skip to content",
    "brand.homeLabel": "Memora Studio — home",
    "nav.mainLabel": "Main navigation",
    "nav.mobileLabel": "Mobile navigation",
    "nav.studio": "Studio",
    "nav.rooms": "Rooms",
    "nav.services": "Services",
    "nav.sessions": "Sessions",
    "nav.plans": "Plans",
    "nav.connect": "Connect",
    "language.label": "Language",
    "language.selectorLabel": "Select language",
    "language.ptLabel": "Use Portuguese",
    "language.enLabel": "Use English",
    "actions.book": "Book",
    "actions.bookTime": "Book a session",
    "actions.bookNow": "Book now",
    "actions.discoverStudio": "Discover the Studio",
    "actions.learnMore": "Learn more",
    "actions.continue": "Continue",
    "actions.gotIt": "Got it",
    "actions.clientAreaLabel": "Access the client area",
    "menu.open": "Open menu",
    "menu.close": "Close menu",
    "toast.clientArea": "The client area is coming soon.",
    "toast.sessionsSoon": "New sessions will be published soon.",
    "toast.faqSoon": "Frequently asked questions are coming soon.",
    "toast.policiesSoon": "Policies are coming soon.",
    "toast.socialUnavailable": "The official link has not been configured yet.",
    "hero.imageAlt": "Memora Studio room with drums, amplifiers, and audio equipment",
    "hero.title": "<span class=\"hero-line hero-line-light\">Your time.</span><span class=\"hero-line hero-line-light\">Your sound.</span><span class=\"hero-line\">Your space.</span>",
    "hero.text": "A fully equipped studio to rehearse,<br>record, create, and connect.<br>Technology, comfort, and organization<br>so you can focus on what matters:<br>the music.",
    "hero.scroll": "Scroll to explore",
    "services.heading": "Memora Studio services",
    "services.rehearsal.title": "Rehearsal",
    "services.rehearsal.text": "Equipped, acoustically treated rooms for you and your band.",
    "services.recording.title": "Recording",
    "services.recording.text": "Record your rehearsal or production with professional quality.",
    "services.audiovisual.title": "Audiovisual",
    "services.audiovisual.text": "Capture your rehearsals and productions in high-quality video.",
    "services.production.title": "Production",
    "services.production.text": "Mixing, editing, and technical support to take your music further.",
    "experience.eyebrow": "The Memora experience",
    "experience.title": "More than a studio.<br><span>An experience.</span>",
    "experience.text": "We take care of the clutter so you can focus on the music. Easy booking, split payments, credits, file organization, and connections with musicians and professionals.",
    "experience.quote": "You take care of the music.<br><strong>The Studio takes care of the rest.</strong>",
    "values.label": "Memora Studio values",
    "values.connection.title": "Connection",
    "values.connection.text": "We connect musicians, services, and opportunities.",
    "values.punctuality.title": "Punctuality",
    "values.punctuality.text": "We value your time and turn it into benefits.",
    "values.innovation.title": "Innovation",
    "values.innovation.text": "Technology that makes everything simpler and more efficient.",
    "values.accessibility.title": "Accessibility",
    "values.accessibility.text": "Music for everyone. A space open to diversity.",
    "values.comfort.title": "Comfort",
    "values.comfort.text": "A welcoming environment where you can feel at ease and create freely.",
    "booking.eyebrow": "Online booking",
    "booking.title": "Book in just<br><span>a few clicks</span>",
    "booking.text": "See available times, choose your room, add services, split the cost with your band, and you’re all set. Everything in one place.",
    "booking.legendLabel": "Booking calendar legend",
    "booking.available": "Available",
    "booking.occupied": "Booked",
    "booking.selected": "Selected",
    "booking.chooseDate": "Choose a date",
    "booking.previousDay": "Previous day",
    "booking.nextDay": "Next day",
    "booking.selectDate": "Select date",
    "booking.scheduleLabel": "Schedule table; swipe horizontally if needed",
    "booking.caption": "Available times by room",
    "booking.yourBooking": "Your booking",
    "booking.total": "Total",
    "booking.timeColumn": "Time",
    "booking.hours": "{count} hours",
    "booking.hour": "{count} hour",
    "booking.timeRange": "{date} — {start} to {end}",
    "booking.confirmationCopy": "{room}, {date}, from {start} to {end} — {total}.",
    "booking.slotLabel": "{room}, {time}, {status}",
    "benefits.heading": "Why choose Memora Studio",
    "benefits.split.title": "Split with the band",
    "benefits.split.text": "Invite the band members and everyone pays their share.",
    "benefits.credits.text": "Earn credits through punctuality, packages, and referrals.",
    "benefits.files.title": "Organized files",
    "benefits.files.text": "All your band’s recordings and media in one place.",
    "benefits.musicians.title": "Find musicians",
    "benefits.musicians.text": "Need someone? Connect with musicians and professionals.",
    "sessions.recordedAt": "Recorded at",
    "sessions.viewAll": "View all sessions",
    "sessions.playLabel": "Play {name}",
    "sessions.imageAlt": "{name} session at Memora Studio",
    "cta.imageAlt": "Guitar and pedals on the Memora Studio stage",
    "cta.eyebrow": "Your next session starts here",
    "cta.title": "Ready to take<br>your music <span>further?</span>",
    "cta.text": "Book now and experience Memora Studio.",
    "footer.description": "A rehearsal, recording, and production studio for musicians who take music seriously.",
    "footer.socialLabel": "Social media",
    "footer.navigation": "Navigation",
    "footer.theStudio": "The Studio",
    "footer.mixing": "Mixing",
    "footer.help": "Help",
    "footer.faq": "Frequently Asked Questions",
    "footer.howItWorks": "How It Works",
    "footer.policies": "Policies",
    "footer.contact": "Contact",
    "footer.directions": "Get directions",
    "footer.copyright": "© 2026 Memora Studio. All rights reserved.",
    "footer.tagline": "Made for those who take music seriously.",
    "rooms.eyebrow": "Professional facilities",
    "rooms.title": "Explore our rooms",
    "rooms.name": "Room {number}",
    "rooms.imageAlt": "{room} at Memora Studio",
    "rooms.feature.drums": "Drums",
    "rooms.feature.amplifiers": "Amplifiers",
    "rooms.feature.pa": "PA",
    "rooms.feature.microphones": "Microphones",
    "rooms.feature.airConditioning": "Air conditioning",
    "rooms.feature.acousticTreatment": "Acoustic treatment",
    "schedule.service.multitrack": "Multitrack recording",
    "schedule.service.cameras": "2 cameras",
    "schedule.service.technician": "Audio engineer",
    "session.type.live": "Live Session",
    "session.type.rehearsalRecording": "Rehearsal + Recording",
    "session.type.rehearsalVideo": "Rehearsal + Video",
    "modal.close": "Close modal",
    "video.title": "Video coming soon.",
    "video.text": "We’re preparing this recording for you.",
    "confirmation.eyebrow": "Booking selected",
    "confirmation.title": "You’re all set to continue.",
    "confirmation.note": "This is a demonstration. Payment and confirmation will be connected to the backend in the future."
  }
};

const LANGUAGE_STORAGE_KEY = "memoraStudioLanguage";

function getStoredLanguage() {
  try {
    return localStorage.getItem(LANGUAGE_STORAGE_KEY) === "en" ? "en" : "pt-BR";
  } catch {
    return "pt-BR";
  }
}

const state = {
  language: getStoredLanguage(),
  date: "2026-05-21",
  roomId: "room-01",
  time: "20:00"
};

function t(key, replacements = {}) {
  const value = translations[state.language]?.[key] ?? translations["pt-BR"][key] ?? key;
  return Object.entries(replacements).reduce(
    (text, [name, replacement]) => text.replaceAll(`{${name}}`, String(replacement)),
    value
  );
}

const studioConfig = {
  assetsBase: "assets/images/",
  contact: {
    phoneDisplay: "(11) 99999-9999",
    phoneHref: "+5511999999999",
    email: "contato@memorastudio.com.br",
    addressLine1: "Rua da Música, 123",
    addressLine2: "São Paulo — SP"
  },
  socialLinks: {
    instagram: "",
    youtube: "",
    whatsapp: ""
  },
  rooms: [
    { id: "room-01", number: "01", image: "sala-01.webp", width: 1280, height: 841, hourlyPrice: 95 },
    { id: "room-02", number: "02", image: "sala-02.webp", width: 1280, height: 834, hourlyPrice: 120 },
    { id: "room-03", number: "03", image: "sala-03.webp", width: 1280, height: 749, hourlyPrice: 145 }
  ],
  roomFeatures: ["drums", "amplifiers", "pa", "microphones", "airConditioning", "acousticTreatment"],
  sessions: [
    { name: "Reckless", typeKey: "session.type.live", image: "session-reckless.webp", width: 1280, height: 770 },
    { name: "Aura", typeKey: "session.type.rehearsalRecording", image: "session-aura.webp", width: 1280, height: 766 },
    { name: "Fourkaos", typeKey: "session.type.live", image: "session-fourkaos.webp", width: 1280, height: 768 },
    { name: "Distance and Belief", typeKey: "session.type.rehearsalVideo", image: "session-distance-and-belief.webp", width: 1280, height: 764 }
  ],
  schedule: {
    dates: ["2026-05-21", "2026-05-22", "2026-05-23", "2026-05-24", "2026-05-25"],
    times: ["18:00", "19:00", "20:00", "21:00", "22:00", "23:00"],
    durationHours: 2,
    services: ["schedule.service.multitrack", "schedule.service.cameras", "schedule.service.technician"],
    servicesPrice: 130,
    occupied: {
      "2026-05-21": { "room-01": ["18:00", "22:00"], "room-02": ["19:00", "20:00"], "room-03": ["21:00", "23:00"] },
      "2026-05-22": { "room-01": ["19:00", "21:00"], "room-02": ["18:00", "23:00"], "room-03": ["20:00"] },
      "2026-05-23": { "room-01": ["20:00", "23:00"], "room-02": ["21:00", "22:00"], "room-03": ["18:00", "19:00"] },
      "2026-05-24": { "room-01": ["18:00", "19:00"], "room-02": ["20:00"], "room-03": ["22:00", "23:00"] },
      "2026-05-25": { "room-01": ["21:00"], "room-02": ["18:00", "22:00"], "room-03": ["19:00", "20:00"] }
    }
  }
};

const elements = {
  header: document.querySelector("[data-header]"),
  menuToggle: document.querySelector("[data-menu-toggle]"),
  mobileMenu: document.querySelector("[data-mobile-menu]"),
  roomsModal: document.getElementById("rooms-modal"),
  videoModal: document.getElementById("video-modal"),
  confirmationModal: document.getElementById("confirmation-modal"),
  roomsGrid: document.getElementById("rooms-grid"),
  sessionsGrid: document.getElementById("sessions-grid"),
  scheduleHead: document.getElementById("schedule-head"),
  scheduleBody: document.getElementById("schedule-body"),
  dateInput: document.querySelector("[data-date-input]"),
  visibleDateLabel: document.getElementById("visible-date-label"),
  toast: document.querySelector("[data-toast-region]")
};

let toastTimer;
const modalTriggers = new WeakMap();

function asset(filename) {
  return `${studioConfig.assetsBase}${filename}`;
}

function escapeHtml(value) {
  return String(value).replace(/[&<>'"]/g, (character) => ({
    "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;"
  })[character]);
}

function roomName(room) {
  return t("rooms.name", { number: room.number });
}

function applyStaticTranslations() {
  document.documentElement.lang = state.language;

  document.querySelectorAll("[data-i18n]").forEach((element) => {
    element.textContent = t(element.dataset.i18n);
  });
  document.querySelectorAll("[data-i18n-html]").forEach((element) => {
    element.innerHTML = t(element.dataset.i18nHtml);
  });

  const translatedAttributes = ["aria-label", "alt", "title", "placeholder", "content"];
  translatedAttributes.forEach((attribute) => {
    const dataAttribute = `data-i18n-${attribute}`;
    document.querySelectorAll(`[${dataAttribute}]`).forEach((element) => {
      element.setAttribute(attribute, t(element.getAttribute(dataAttribute)));
    });
  });

  if (elements.menuToggle.getAttribute("aria-expanded") === "true") {
    elements.menuToggle.setAttribute("aria-label", t("menu.close"));
  }

  document.querySelectorAll("[data-language]").forEach((button) => {
    const active = button.dataset.language === state.language;
    button.setAttribute("aria-pressed", String(active));
    button.classList.toggle("is-active", active);
  });
}

function setLanguage(language, { persist = true } = {}) {
  if (!translations[language]) return;
  state.language = language;
  if (persist) {
    try {
      localStorage.setItem(LANGUAGE_STORAGE_KEY, language);
    } catch {
      // The page still works when storage is unavailable.
    }
  }
  applyStaticTranslations();
  renderRooms();
  renderSessions();
  renderSchedule();
  updateSummary();
}

function showToast(message) {
  clearTimeout(toastTimer);
  elements.toast.textContent = message;
  elements.toast.classList.add("is-visible");
  toastTimer = window.setTimeout(() => elements.toast.classList.remove("is-visible"), 3000);
}

function syncBodyLock() {
  const dialogOpen = Boolean(document.querySelector("dialog[open]"));
  const menuOpen = elements.menuToggle.getAttribute("aria-expanded") === "true";
  document.body.classList.toggle("is-locked", dialogOpen || menuOpen);
}

function openModal(dialog, trigger) {
  if (!dialog || dialog.open) return;
  modalTriggers.set(dialog, trigger || document.activeElement);
  dialog.showModal();
  syncBodyLock();
  window.requestAnimationFrame(() => {
    const preferred = dialog.querySelector("[data-close-modal]");
    preferred?.focus();
  });
}

function closeModal(dialog) {
  if (dialog?.open) dialog.close();
}

function getFocusable(container) {
  return [...container.querySelectorAll('a[href], button:not([disabled]), input:not([disabled]), [tabindex]:not([tabindex="-1"])')]
    .filter((element) => !element.hasAttribute("hidden") && element.getClientRects().length);
}

function trapDialogFocus(event) {
  if (event.key !== "Tab") return;
  const dialog = event.currentTarget;
  const focusable = getFocusable(dialog);
  if (!focusable.length) return;
  const first = focusable[0];
  const last = focusable[focusable.length - 1];
  if (event.shiftKey && document.activeElement === first) {
    event.preventDefault();
    last.focus();
  } else if (!event.shiftKey && document.activeElement === last) {
    event.preventDefault();
    first.focus();
  }
}

function renderRooms() {
  elements.roomsGrid.innerHTML = studioConfig.rooms.map((room) => `
    <article class="room-card">
      <img src="${asset(room.image)}" width="${room.width}" height="${room.height}" alt="${escapeHtml(t("rooms.imageAlt", { room: roomName(room) }))}" loading="lazy" decoding="async">
      <div class="room-content">
        <h3>${escapeHtml(roomName(room))}</h3>
        <ul>${studioConfig.roomFeatures.map((feature) => `<li>${escapeHtml(t(`rooms.feature.${feature}`))}</li>`).join("")}</ul>
      </div>
    </article>
  `).join("");
}

function renderSessions() {
  elements.sessionsGrid.innerHTML = studioConfig.sessions.map((session) => `
    <article class="session-card reveal">
      <button type="button" aria-label="${escapeHtml(t("sessions.playLabel", { name: session.name }))}" data-session="${escapeHtml(session.name)}">
        <span class="session-image">
          <img src="${asset(session.image)}" width="${session.width}" height="${session.height}" alt="${escapeHtml(t("sessions.imageAlt", { name: session.name }))}" loading="lazy" decoding="async">
          <span class="play-button" aria-hidden="true">▶</span>
        </span>
        <span class="session-info"><h3>${escapeHtml(session.name)}</h3><p>${escapeHtml(t(session.typeKey))}</p></span>
      </button>
    </article>
  `).join("");

  elements.sessionsGrid.querySelectorAll("[data-session]").forEach((button) => {
    button.addEventListener("click", () => openModal(elements.videoModal, button));
  });
}

function parseLocalDate(dateValue) {
  return new Date(`${dateValue}T12:00:00`);
}

function formatLongDate(dateValue) {
  return new Intl.DateTimeFormat(state.language, { weekday: "long", day: "2-digit", month: "long" }).format(parseLocalDate(dateValue));
}

function formatShortDate(dateValue) {
  return new Intl.DateTimeFormat(state.language, { day: "2-digit", month: "2-digit" }).format(parseLocalDate(dateValue));
}

function endTime(start, duration) {
  const [hours, minutes] = start.split(":").map(Number);
  const endHours = (hours + duration) % 24;
  return `${String(endHours).padStart(2, "0")}:${String(minutes).padStart(2, "0")}`;
}

function isOccupied(roomId, time) {
  return studioConfig.schedule.occupied[state.date]?.[roomId]?.includes(time) || false;
}

function renderSchedule() {
  elements.scheduleHead.innerHTML = `<th scope="col">${escapeHtml(t("booking.timeColumn"))}</th>${studioConfig.rooms.map((room) => `<th scope="col">${escapeHtml(roomName(room))}</th>`).join("")}`;
  elements.scheduleBody.innerHTML = studioConfig.schedule.times.map((time) => `
    <tr>
      <td>${time}</td>
      ${studioConfig.rooms.map((room) => {
        const occupied = isOccupied(room.id, time);
        const selected = state.roomId === room.id && state.time === time;
        const status = occupied ? t("booking.occupied") : t("booking.available");
        const label = t("booking.slotLabel", { room: roomName(room), time, status: status.toLocaleLowerCase(state.language) });
        return `<td><button class="slot${selected ? " is-selected" : ""}" type="button" data-room="${room.id}" data-time="${time}" aria-label="${escapeHtml(label)}" aria-pressed="${selected}" ${occupied ? "disabled" : ""}>${occupied ? escapeHtml(t("booking.occupied")) : time}</button></td>`;
      }).join("")}
    </tr>
  `).join("");

  elements.scheduleBody.querySelectorAll(".slot:not(:disabled)").forEach((slot) => {
    slot.addEventListener("click", () => {
      state.roomId = slot.dataset.room;
      state.time = slot.dataset.time;
      renderSchedule();
      updateSummary();
      document.querySelector(`[data-room="${state.roomId}"][data-time="${state.time}"]`)?.focus();
    });
  });

  elements.visibleDateLabel.textContent = formatLongDate(state.date);
  elements.dateInput.value = state.date;
}

function updateSummary() {
  const room = studioConfig.rooms.find((item) => item.id === state.roomId);
  const duration = studioConfig.schedule.durationHours;
  const total = room.hourlyPrice * duration + studioConfig.schedule.servicesPrice;
  const localizedRoomName = roomName(room);
  const end = endTime(state.time, duration);
  const localizedTotal = total.toLocaleString(state.language, { style: "currency", currency: "BRL" });
  document.getElementById("summary-room").textContent = localizedRoomName;
  document.getElementById("summary-time").textContent = t("booking.timeRange", { date: formatShortDate(state.date), start: state.time, end });
  document.getElementById("summary-duration").textContent = t(duration === 1 ? "booking.hour" : "booking.hours", { count: duration });
  document.getElementById("summary-services").innerHTML = studioConfig.schedule.services.map((service) => `<li>${escapeHtml(t(service))}</li>`).join("");
  document.getElementById("summary-total").textContent = localizedTotal;
  document.getElementById("confirmation-copy").textContent = t("booking.confirmationCopy", { room: localizedRoomName, date: formatShortDate(state.date), start: state.time, end, total: localizedTotal });
}

function setDate(dateValue) {
  if (!studioConfig.schedule.dates.includes(dateValue)) return;
  state.date = dateValue;
  if (isOccupied(state.roomId, state.time)) {
    const available = studioConfig.schedule.times.flatMap((time) => studioConfig.rooms.map((room) => ({ room, time }))).find(({ room, time }) => !isOccupied(room.id, time));
    state.roomId = available.room.id;
    state.time = available.time;
  }
  renderSchedule();
  updateSummary();
}

function moveDate(step) {
  const dates = studioConfig.schedule.dates;
  const index = dates.indexOf(state.date);
  const nextIndex = Math.max(0, Math.min(dates.length - 1, index + step));
  setDate(dates[nextIndex]);
}

function setContactDetails() {
  const phone = document.getElementById("contact-phone");
  const email = document.getElementById("contact-email");
  phone.textContent = studioConfig.contact.phoneDisplay;
  phone.href = `tel:${studioConfig.contact.phoneHref}`;
  email.textContent = studioConfig.contact.email;
  email.href = `mailto:${studioConfig.contact.email}`;
  document.getElementById("contact-address").innerHTML = `${escapeHtml(studioConfig.contact.addressLine1)}<br>${escapeHtml(studioConfig.contact.addressLine2)}`;
}

function closeMobileMenu({ restoreFocus = false } = {}) {
  if (elements.mobileMenu.hidden) return;
  elements.mobileMenu.hidden = true;
  elements.menuToggle.setAttribute("aria-expanded", "false");
  elements.menuToggle.setAttribute("aria-label", t("menu.open"));
  syncBodyLock();
  if (restoreFocus) elements.menuToggle.focus();
}

function toggleMobileMenu() {
  const willOpen = elements.mobileMenu.hidden;
  elements.mobileMenu.hidden = !willOpen;
  elements.menuToggle.setAttribute("aria-expanded", String(willOpen));
  elements.menuToggle.setAttribute("aria-label", willOpen ? t("menu.close") : t("menu.open"));
  syncBodyLock();
  if (willOpen) elements.mobileMenu.querySelector("a, button")?.focus();
}

function initializeNavigation() {
  elements.menuToggle.addEventListener("click", toggleMobileMenu);
  elements.mobileMenu.querySelectorAll("a").forEach((link) => link.addEventListener("click", () => closeMobileMenu()));
  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape" && !elements.mobileMenu.hidden) closeMobileMenu({ restoreFocus: true });
  });

  const sectionIds = ["studio", "servicos", "sessions", "agendamento", "connect"];
  const sectionObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (!entry.isIntersecting) return;
      document.querySelectorAll("[data-nav-link]").forEach((link) => {
        const active = link.getAttribute("href") === `#${entry.target.id}`;
        link.classList.toggle("is-active", active);
        if (active) link.setAttribute("aria-current", "page");
        else link.removeAttribute("aria-current");
      });
    });
  }, { rootMargin: "-30% 0px -60%", threshold: 0 });
  sectionIds.forEach((id) => {
    const section = document.getElementById(id);
    if (section) sectionObserver.observe(section);
  });
}

function initializeDialogs() {
  document.querySelectorAll("[data-open-rooms]").forEach((button) => {
    button.addEventListener("click", () => {
      closeMobileMenu();
      openModal(elements.roomsModal, button);
    });
  });
  document.querySelector("[data-confirm-booking]").addEventListener("click", (event) => openModal(elements.confirmationModal, event.currentTarget));

  document.querySelectorAll("dialog").forEach((dialog) => {
    dialog.addEventListener("keydown", trapDialogFocus);
    dialog.addEventListener("click", (event) => {
      if (event.target === dialog) closeModal(dialog);
    });
    dialog.addEventListener("close", () => {
      syncBodyLock();
      modalTriggers.get(dialog)?.focus();
    });
    dialog.addEventListener("cancel", (event) => {
      event.preventDefault();
      closeModal(dialog);
    });
    dialog.querySelectorAll("[data-close-modal]").forEach((button) => button.addEventListener("click", () => closeModal(dialog)));
  });
  document.addEventListener("keydown", (event) => {
    if (event.key !== "Escape") return;
    const openDialog = document.querySelector("dialog[open]");
    if (!openDialog) return;
    event.preventDefault();
    closeModal(openDialog);
  }, true);
}

function initializeActions() {
  document.querySelectorAll("[data-i18n-toast]").forEach((button) => button.addEventListener("click", () => showToast(t(button.dataset.i18nToast))));
  document.querySelectorAll("[data-social]").forEach((button) => {
    button.addEventListener("click", () => {
      const url = studioConfig.socialLinks[button.dataset.social];
      if (url) window.open(url, "_blank", "noopener,noreferrer");
      else showToast(t("toast.socialUnavailable"));
    });
  });
  document.querySelector("[data-directions]").addEventListener("click", () => showToast(`${studioConfig.contact.addressLine1} · ${studioConfig.contact.addressLine2}`));
  document.querySelector("[data-date-prev]").addEventListener("click", () => moveDate(-1));
  document.querySelector("[data-date-next]").addEventListener("click", () => moveDate(1));
  elements.dateInput.addEventListener("change", (event) => setDate(event.target.value));
}

function initializeLanguageSwitcher() {
  document.querySelectorAll("[data-language]").forEach((button) => {
    button.addEventListener("click", () => setLanguage(button.dataset.language));
  });
}

function initializeScrollEffects() {
  const onScroll = () => elements.header.classList.toggle("is-scrolled", window.scrollY > 18);
  onScroll();
  window.addEventListener("scroll", onScroll, { passive: true });

  if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
    document.querySelectorAll(".reveal").forEach((item) => item.classList.add("is-visible"));
    return;
  }
  const revealObserver = new IntersectionObserver((entries, observer) => {
    entries.forEach((entry) => {
      if (!entry.isIntersecting) return;
      entry.target.classList.add("is-visible");
      observer.unobserve(entry.target);
    });
  }, { threshold: .12 });
  document.querySelectorAll(".reveal").forEach((item) => revealObserver.observe(item));
}

applyStaticTranslations();
renderRooms();
renderSessions();
renderSchedule();
updateSummary();
setContactDetails();
initializeNavigation();
initializeDialogs();
initializeActions();
initializeLanguageSwitcher();
initializeScrollEffects();
